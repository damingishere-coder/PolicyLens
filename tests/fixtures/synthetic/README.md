# 纯合成测试资料

本目录中的测试资料只描述不存在的 `SYNTHETIC Care Alpha` 与 `SYNTHETIC Care Beta`。名称、机构、版本、金额和条款均为从零编写的虚构值，不来自任何真实家庭、真实保单或真实保险产品。

运行 `python scripts/create_synthetic_fixtures.py` 会生成两个仅用于测试的文本型 PDF。生成文件不得改名为真实保单，也不得混入私人资料。
